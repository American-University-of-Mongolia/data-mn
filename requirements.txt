# Data.mn Python Dependencies
#
# Create conda environment:
#   conda create -n datamn python=3.12
#   conda activate datamn
#   pip install -r requirements.txt
#
# Or with conda directly:
#   conda env create -f environment.yml

# Core dependencies
requests>=2.31.0          # HTTP client for API calls
pandas>=2.1.0             # Data manipulation and CSV handling
openpyxl>=3.1.0           # Excel file support (pandas uses this for .xlsx)
PyYAML>=6.0               # MDX frontmatter validation

# PDF extraction (for MRPAM and other PDF sources)
pdfplumber>=0.10.0        # PDF table extraction

# Web scraping (for MRPAM report page scraping)
beautifulsoup4>=4.12.0    # HTML parsing for PDF link discovery

# Optional: Development tools
# pytest>=7.4.0           # Testing (uncomment if needed)
# black>=23.0.0           # Code formatting (uncomment if needed)
